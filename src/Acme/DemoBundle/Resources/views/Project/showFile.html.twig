{% extends '::base.html.twig' %}

{% block body -%}
    <h1>File</h1>

    <table class="record_properties">
        <tbody>
            {% for item in entity.getLinksText %}
                <tr>
                    <td>{{ item.name }}|{{ item.url }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
